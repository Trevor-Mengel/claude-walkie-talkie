# CLAUDE.md

**See [AGENTS.md](./AGENTS.md).**

All guidance for working in this repository lives there, as universal instructions for any coding agent — Claude Code, Codex, OMP, Cursor, or anything else. This file exists only because Claude Code reads `CLAUDE.md` by convention; it is deliberately a pointer rather than a copy, so the two can never drift.

Start with the **"Status and handoff"** section at the top of `AGENTS.md`: it records the current branch and HEAD, what P0 and P1 actually do and do not implement, and the traps that have already cost this project real defects.
